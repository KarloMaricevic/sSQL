/* fn select(select: &SqlStatement::Select) {
    
    // search for appropriate index if there is one
    // demand index tree with that index 
    // get specic page gooten from that index 
    // load and parse
} */